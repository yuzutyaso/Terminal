# ベースイメージとしてNginxの軽量版（Alpine Linuxベース）を使用します。
# これにより、イメージのサイズを最小限に抑えることができます。
FROM nginx:1.25.3-alpine

# コンテナ内に`app`というディレクトリを作成し、作業ディレクトリとして設定します。
WORKDIR /app

# ローカルのHTML、CSS、JavaScriptファイル（現在のディレクトリにあるファイル）を、
# コンテナ内のNginxが静的ファイルを読み込むディレクトリにコピーします。
COPY . /usr/share/nginx/html

# Nginxはデフォルトでポート80をリッスンするように設定されているため、
# EXPOSEコマンドでポート80を公開することを明示します。
EXPOSE 80

# コンテナが起動したときに、Nginxをフォアグラウンドで実行するコマンドを設定します。
# これにより、コンテナが継続的に動作します。
CMD ["nginx", "-g", "daemon off;"]
